//var chalk = require ('chalk');  //This is an ES5 implementation
import chalk from 'chalk';  // This is new in ES6, Transpile with Babel to use it

console.log(chalk.green('starting the app')); // eslint-disable-line no-console
